#json_format:
#  - show version
text_format:

  - show ip bgp summary vrf all
  - show bgp evpn summary
  - show bgp evpn
  - show ip route vrf all
  - show bgp evpn route-type auto-discovery detail # type-1
  - show bgp evpn route-type ip-prefix ipv4 detail # type-5
  - show bgp evpn route-type mac-ip detail # type-2
  - show bgp evpn route-type ethernet-segment detail # type-4

